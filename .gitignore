node_modules/
dist/
*.log
.env
.env.*
.DS_Store

# Vercel CLI local link + build cache.
.vercel/

# Audit screenshots are regenerated by `node scripts/audit-ui.mjs`. The script
# is the artifact worth versioning; its 7 PNGs per run are not.
audit/

# Never commit key material. Olex writes no keystore today, but this guard
# goes in before the wallet tools land, not after.
*.key
keystore.json
.olex/
